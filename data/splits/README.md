# Particiones (semilla 2024)

Coloca aquí las particiones **anonimizadas** (mismo tratamiento que el subcorpus):

- `train.csv` (963), `val.csv` (117), `test.csv` (120)

Estratificación por clase y por fase temporal. Sin fuga de `msg_id` entre
particiones. Antes de subirlas, aplícales la misma anonimización que a
`data/subcorpus/subcorpus_anotado_FINAL_anon.csv`.
