# ── DATOS SENSIBLES — NUNCA versionar ──────────────────────────────
# Tabla de equivalencia de la seudonimización (reidentifica usuarios).
# Permanece bajo control del investigador, fuera del repositorio.
mapa_seudonimos.csv
**/mapa_seudonimos.csv
# Corpus y subcorpus con texto crudo sin anonimizar
subcorpus_anotado_FINAL.csv
*_raw.csv
corpus_completo*.csv

# ── Python ─────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# ── Entorno / sistema ──────────────────────────────────────────────
.DS_Store
.env
*.log

# ── Salidas de entrenamiento voluminosas (regenerables) ────────────
# Se versiona solo el mejor modelo (model/best_model/) vía Git LFS;
# los checkpoints intermedios y logs de la malla no se publican.
runs/
checkpoints/
wandb/
